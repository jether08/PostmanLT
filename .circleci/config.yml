version: 2.1
orbs:
  newman: postman/newman@0.0.2
jobs:
  build:
  newman-collection-run:
    executor: newman/postman-newman-docker
    steps:
      - checkout
      - newman/newman-run:
          collection: ./Lfiles/FIles/Postman/Repo/PostmanLT/ESL API.postman_collection.json

          